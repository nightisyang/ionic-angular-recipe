import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.splish.ionicangularrecipe',
  appName: 'ionic-angular-recipe',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
